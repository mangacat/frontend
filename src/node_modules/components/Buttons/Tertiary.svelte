<button on:click={() => dispatch('click')} class="{_class} text-gray-800 hover:text-gray-600 dark:text-gray-200 dark:hover:text-gray-400 font-semibold py-2 px-4" type="button">
    <slot />
</button>

<script>
    import { createEventDispatcher } from 'svelte'

    const dispatch = createEventDispatcher()

    let _class
    export { _class as class }
</script>