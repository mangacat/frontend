<button on:click={() => dispatch('click')} class="{_class} bg-gray-300 dark:bg-gray-400 hover:shadow-lg text-gray-800 font-semibold py-2 px-4 rounded" type="button">
    <slot />
</button>

<script>
    import { createEventDispatcher } from 'svelte'

    const dispatch = createEventDispatcher()

    let _class
    export { _class as class }
</script>