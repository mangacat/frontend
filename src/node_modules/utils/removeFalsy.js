export const removeFalsy = object => {
	const newObject = {}
	Object.keys(object).forEach(property => {
		if (object[property] || typeof object[property] == 'boolean') newObject[property] = object[property]
	})
	return newObject
}