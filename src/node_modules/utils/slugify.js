import slugify from '@sindresorhus/slugify'

const processor = text =>
	slugify(text, {
		customReplacements: [
			['&', 'and']
		],
		decamelize: false,
		lowercase: true,
		separator: '-'
	})

export { processor as slugify }